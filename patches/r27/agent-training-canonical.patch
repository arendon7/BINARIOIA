--- a/apps/03_agente_ia_whatsapp/agent_studio/service.py
+++ b/apps/03_agente_ia_whatsapp/agent_studio/service.py
@@
-import argparse, json, mimetypes, os, sys, traceback
+import argparse, base64, json, mimetypes, os, sys, tempfile, traceback
@@
 from common.secret_store import secret_status, set_secret, delete_secret
+from common import project_center as global_project_center
+from r26.r26_core.agent_training import import_training_file, csv_template, jsonl_template
+
+def import_training_into_project(pr,filename,raw):
+    # Persist the original source under the canonical physical project's training/ folder.
+    # Q&A supplied explicitly by the user becomes approved FAQ knowledge.
+    # Free-form document chunks remain disabled until the user reviews/approves them.
+    ...
@@
+            if p=='/api/training/template.csv':return self._text(csv_template(),ctype='text/csv; charset=utf-8')
+            if p=='/api/training/template.jsonl':return self._text(jsonl_template(),ctype='application/x-ndjson; charset=utf-8')
@@
-                    for k in ['name','status','current_step','business','knowledge','intents','variables','guardrails','flow','model','channel','tests','publish','observations']:
+                    for k in ['name','status','current_step','business','knowledge','knowledge_documents','intents','variables','guardrails','flow','model','channel','tests','publish','observations']:
@@
+                if action=='training-import':
+                    # JSON/base64 upload, max 12 MiB, parsed by the existing R26 agent-training contract.
+                    ...
+                if action=='training-approve-docs':
+                    # Explicit approval gate for imported document chunks.
+                    ...
--- a/apps/03_agente_ia_whatsapp/agent_studio/store.py
+++ b/apps/03_agente_ia_whatsapp/agent_studio/store.py
@@
       "knowledge":[],
+      "knowledge_documents":[],
--- a/apps/03_agente_ia_whatsapp/agent_studio/simulator.py
+++ b/apps/03_agente_ia_whatsapp/agent_studio/simulator.py
@@
     for k in project.get('knowledge',[]):
@@
+    for d in project.get('knowledge_documents',[]):
+        if d.get('enabled',False) is False:continue
+        # Approved document chunks participate in retrieval; disabled chunks never do.
+        ...
@@
-        approved=[...]
+        approved=[...]
+        approved_docs=[... for d in project.get("knowledge_documents",[]) if d.get("enabled",False) and d.get("text")]
+        approved=(approved+approved_docs)[:30]
--- a/apps/03_agente_ia_whatsapp/agent_studio/exporter.py
+++ b/apps/03_agente_ia_whatsapp/agent_studio/exporter.py
@@
-      'knowledge':len(project.get('knowledge') or [])>=1,
+      'knowledge':bool(project.get('knowledge') or [d for d in project.get('knowledge_documents',[]) if d.get('enabled')]),
@@
-      'knowledge':{'faq':project.get('knowledge',[]),'hours':...},
+      'knowledge':{'faq':project.get('knowledge',[]),'documents':project.get('knowledge_documents',[]),'hours':...},
--- a/apps/03_agente_ia_whatsapp/ui/index.html
+++ b/apps/03_agente_ia_whatsapp/ui/index.html
@@
 <section class="panel step-panel" data-step="knowledge" hidden>
+  <!-- Canonical training import: TXT/MD/PDF/DOCX and CSV/JSON/JSONL Q&A templates. -->
+  <div class="trainingImport">...</div>
+  <div id="trainingDocuments"></div>
 </section>
--- a/apps/03_agente_ia_whatsapp/ui/app.js
+++ b/apps/03_agente_ia_whatsapp/ui/app.js
@@
+function renderTrainingDocuments(){/* review + explicit approval */}
+async function importTraining(){/* base64 upload to the current Agent Studio project */}
+async function approveTrainingDocs(enabled=true){/* explicit truth gate */}
