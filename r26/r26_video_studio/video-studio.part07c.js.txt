  wireTransformBox();

  if ($("quickImportInput")) $("quickImportInput").onchange = async e => { if (e.target.files.length) { snapshot(); await addFiles(e.target.files); e.target.value=""; } };
  if ($("quickTranscribeBtn")) $("quickTranscribeBtn").onclick = transcribePrimaryVideo;
  if ($("quickClipsBtn")) $("quickClipsBtn").onclick = generateClipCandidates;
  if ($("quickRenderBtn")) $("quickRenderBtn").onclick = renderProject;
  $("assetInput").onchange = async e => { if (e.target.files.length) { snapshot(); await addFiles(e.target.files); e.target.value = ""; } };
  const dz = $("dropzone"); ["dragenter", "dragover"].forEach(n => dz.addEventListener(n, e => { e.preventDefault(); dz.classList.add("drag"); })); ["dragleave", "drop"].forEach(n => dz.addEventListener(n, e => { e.preventDefault(); dz.classList.remove("drag"); })); dz.addEventListener("drop", async e => { if (e.dataTransfer.files.length) { snapshot(); await addFiles(e.dataTransfer.files); } });
  document.querySelectorAll("[data-filter]").forEach(b => b.onclick = () => { filter = b.dataset.filter; document.querySelectorAll("[data-filter]").forEach(x => x.classList.toggle("active", x === b)); renderAssets(); });
  $("projectName").onchange = e => { snapshot(); state.name = e.target.value || "Video nuevo"; renderSequences(); };
  $("newProjectDiskBtn").onclick = newDiskProject; $("openProjectBtn").onclick = () => openProjectFolder("root"); $("openExportsBtn").onclick = () => openProjectFolder("exports"); $("newClipBtn").onclick = newClip;
  $("deleteBtn").onclick = removeSelected; $("duplicateBtn").onclick = duplicateSelected; $("splitBtn").onclick = splitSelected; $("splitTimelineBtn").onclick = splitSelected; $("keyframeBtn").onclick = addKeyframe; $("audioKeyframeBtn").onclick = addAudioKeyframe; $("captionQuickBtn").onclick=addSubtitleSample; $("fitTimelineBtn").onclick=fitTimeline; $("undoBtn").onclick = undo; $("redoBtn").onclick = redo; $("playBtn").onclick = togglePlay; $("prevBtn").onclick = () => setPlayhead(0); $("nextBtn").onclick = () => setPlayhead(seq().duration); $("zoomRange").oninput = e => { pxSec = Number(e.target.value); renderTimeline(); };
  $("keyIdeasToggle").onchange = e => { snapshot(); track("key_ideas").enabled = e.target.checked; state.settings.key_ideas_default = e.target.checked; if (e.target.checked && !track("key_ideas").items.length) addKeyIdea(); else renderAll(); };
  $("subtitlesToggle").onchange = e => { snapshot(); track("subtitles").enabled = e.target.checked; renderAll(); };
  $("normalizeToggle").onchange = e => { snapshot(); state.settings.audio.auto_normalize = e.target.checked; }; $("duckToggle").onchange = e => { snapshot(); state.settings.audio.duck_music_under_voice = e.target.checked; };
  $("denoiseToggle").onchange = e => { snapshot(); state.settings.audio.voice_denoise = e.target.checked; }; $("compressToggle").onchange = e => { snapshot(); state.settings.audio.voice_compression = e.target.checked; }; $("limiterToggle").onchange = e => { snapshot(); state.settings.audio.limiter = e.target.checked; };
  $("targetLufs").onchange = e => { snapshot(); state.settings.audio.target_lufs = clamp(Number(e.target.value), -24, -8); syncControls(); }; $("truePeak").onchange = e => { snapshot(); state.settings.audio.true_peak_db = clamp(Number(e.target.value), -6, -.1); syncControls(); }; $("musicGain").onchange = e => { snapshot(); state.settings.audio.music_gain = clamp(Number(e.target.value), 0, 2); syncControls(); };
  $("analyzeLoudnessBtn").onclick = () => { const [, item] = selected ? findItem(selected) : [null,null]; const asset = item?.asset_id ? state.assets.find(a => a.id === item.asset_id) : primaryVideoAsset(); if (!asset) return toast("Selecciona audio/video o agrega video principal", true); analyzeLoudnessById(asset.id); };
  $("proxyPrimaryBtn").onclick = () => { const asset = primaryVideoAsset(); if (!asset) return toast("Agrega video principal primero", true); createProxyById(asset.id); }; $("proxyToggle").onchange = () => renderPreview(); $("hardwareAccelerationSelect").onchange = e => { snapshot(); state.settings.export.hardware_acceleration=e.target.value; dirty(); refreshEngineStatus(false); };
  $("snapToggle").onchange = e => { snapshot(); seq().settings.snap = e.target.checked; }; $("safeZonesToggle").onchange = e => { snapshot(); seq().settings.safe_zones = e.target.checked; syncControls(); };
  if ($("timelineEditMode")) $("timelineEditMode").onchange = e => { snapshot(); state.settings.timeline_edit_mode = e.target.value; dirty(); toast(e.target.value === "ripple" ? "Edición ripple: al mover un clip se desplaza lo posterior" : e.target.value === "magnetic" ? "Edición magnética activa" : "Edición libre activa"); };
  if ($("publishPresetSelect")) $("publishPresetSelect").onchange = async e => {
    const preset = e.target.value; if (!preset) { snapshot(); state.settings.export.publish_preset = ""; syncControls(); dirty(); return; }
    try { const r = await apiJson("/api/export/preset", {method:"POST", headers:{"Content-Type":"application/json"}, body:JSON.stringify({project:state, sequence_id:seq().id, preset_id:preset})}); snapshot(); state=r.project; ensureShape(); renderAll(); dirty(); toast(`${r.preset.label || preset}: exportación configurada`); }
    catch(err) { toast(`No pude aplicar preset: ${err.message}`, true); syncControls(); }
  };
  document.querySelectorAll("[data-ratio]").forEach(b => b.onclick = () => { snapshot(); seq().aspect_ratio = b.dataset.ratio; syncControls(); renderPreview(); });
  $("simpleModeBtn").onclick = async () => { snapshot(); const mode = await persistEditingMode(state.settings.editing_mode === "simple" ? "pro" : "simple"); toast(mode === "simple" ? "Modo simple: Importar → Transcribir → Clips → Renderizar" : "Modo pro: transformación, color, keyframes y automatización completos"); };
  $("fitBtn").onclick = () => { if (!selected) return toast("Selecciona un visual", true); const [, item] = findItem(selected); if (!item || !VISUAL_TRACKS.has(item.track)) return; snapshot(); item.transform = baseTransform(); renderAll(); };
  $("exportBtn").onclick = renderProject; $("renderAllClipsBtn").onclick = renderAllClips; $("exportPlanBtn").onclick = exportProject; $("openLastRenderBtn").onclick = () => openProjectFolder("exports"); $("engineStatusBtn").onclick = () => refreshEngineStatus(true); document.querySelectorAll("[data-smart]").forEach(b => b.onclick = () => smart(b.dataset.smart));
  $("detectScenesBtn").onclick = () => detectScenesPrimary(true); $("detectSilenceBtn").onclick=()=>detectSilencesPrimary(true); $("applySilenceBtn").onclick=applyDetectedSilences; $("sceneCollapseBtn").onclick = () => { snapshot(); sceneStore().collapsed=!sceneStore().collapsed; renderSceneStrip(); dirty(); };
  $("subtitleImportInput").onchange = async e => { if (e.target.files[0]) await importSubtitleFile(e.target.files[0]); e.target.value = ""; }; $("transcribeBtn").onclick = transcribePrimaryVideo; $("generateClipCandidatesBtn").onclick = generateClipCandidates;
